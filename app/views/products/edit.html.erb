<h1>Edit Product</h1>
<%= form_tag "/products/#{@product.id}", method: :patch do %>
  <div class="form-group">
    Name: <input type='text' class="form-control" value="<%= @product.name %>" name="form_name">
  </div>
  <div class="form-group">
    Price: <input type='text' class="form-control" value="<%= @product.price %>" name="form_price">
  </div>
  <div class="form-group">
    Description: <input type='text' class="form-control" value="<%= @product.description %>" name="form_description">
  </div>
  <div class="form-group">
    Supplier Name: <input type='text' class="form-control" value="<%= @product.supplier_id%>" name="form_supplier">
  </div>

  <!-- <div class="parent">
  <%@product.images.each do |image|%>
    <img class=product_image src="<%= image.url %>">
      <div class="form-group">
        Url: <input class="form-control" type='text' name="url" value="<%=@product.images.first.url%>">
      </div>
      <div class="form-group">
        Product ID: <input class="form-control" type='text' name="product_id" value="<%=@product.images.sample.product_id%>">
      </div>
  <% end %>
  </div> -->

  <input type ="submit" value="Update Product Info">
<% end %>